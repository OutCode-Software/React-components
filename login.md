In login component we can set image in left component and login , register form in right compoment
```
<div className={style.loginWrapper}>
   <LeftLogin loginImage={login} />
   <RightLogin />
</div>
```
